-- require "prototypes.metallurgy.liquid-crafting.functions"
require "prototypes.internal-config"

function Iron(AMOUNT)
return {type="fluid", name="molten-iron", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Copper(AMOUNT)
return {type="fluid", name="molten-copper", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Steel(AMOUNT)
return {type="fluid", name="molten-carbonated-iron", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Zinc(AMOUNT)
return {type="fluid", name="molten-zinc", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Lead(AMOUNT)
return {type="fluid", name="molten-lead", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Gold(AMOUNT)
return {type="fluid", name="molten-gold", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Silver(AMOUNT)
return {type="fluid", name="molten-silver", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Tungsten(AMOUNT)
return {type="fluid", name="molten-tungsten", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Tin(AMOUNT)
return {type="fluid", name="molten-tin", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Ardite(AMOUNT)
return {type="fluid", name="molten-ardite", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Cobalt(AMOUNT)
return {type="fluid", name="molten-cobalt", amount=(AMOUNT*Liquid_Crafting_Factor)}
end

function Stone(AMOUNT)
return {type="fluid", name="sludge", amount=(AMOUNT*Liquid_Crafting_Factor)}
end